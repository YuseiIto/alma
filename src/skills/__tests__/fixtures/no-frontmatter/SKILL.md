# No Frontmatter Skill

This file is valid markdown but has no frontmatter at all. There are no --- delimiters to define YAML metadata.

The file starts directly with markdown content, which violates the SKILL.md format requirement that frontmatter must be present.

## Content

Without frontmatter, required fields like "name" and "description" cannot be defined.
